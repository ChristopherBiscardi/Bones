define([
	'jQuery',
  'Underscore',
  'Backbone',
], function($, _, Backbone) {
  var appModel = Backbone.Model.extend({
	defaults: {
	},
  initialize: function(){
  }
    
  });
  return appModel;

});